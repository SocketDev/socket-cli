# Suppress pnpm build script warnings.
ignore-scripts=true

# Disable workspace linking - use pinned dependencies from registry.
link-workspace-packages=false
loglevel=error
prefer-workspace-packages=false