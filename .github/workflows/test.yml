name: Tests

on:
  push:
    branches: [main, v1.x]
    tags: ['*']
  pull_request:
    branches: [main, v1.x]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    uses: SocketDev/socket-registry/.github/workflows/test.yml@main
