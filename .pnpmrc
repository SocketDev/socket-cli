# Security: Allowlist for native binaries
only-built-dependencies[]=@appthreat/sqlite3
only-built-dependencies[]=esbuild
only-built-dependencies[]=oxc-resolver
only-built-dependencies[]=unrs-resolver

# Enable pre/post scripts for the main project (e.g., prepare -> husky)
enable-pre-post-scripts=true

# Dependency management
minimumReleaseAge=10080
auto-install-peers=true
strict-peer-dependencies=false
save-exact=true